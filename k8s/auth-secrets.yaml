apiVersion: v1
kind: Secret
metadata:
  name: auth-secrets
  namespace: ${NAMESPACE}
type: Opaque
stringData:
  DB_URL: "${DB_URL}"
  DB_USER: "${DB_USER}"
  DB_PASSWORD: "${DB_PASSWORD}"
  JWT_SECRET: "${JWT_SECRET}"