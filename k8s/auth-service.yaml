apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: ${NAMESPACE}
spec:
  type: NodePort
  selector:
    app: tc-oficina-auth
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: ${NODE_PORT_AUTH}